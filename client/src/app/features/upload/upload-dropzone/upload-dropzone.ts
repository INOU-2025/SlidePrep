import { Component } from '@angular/core';

// This component is superseded by CreateModalComponent and is no longer routed.
@Component({
  selector: 'app-upload-dropzone-legacy',
  imports: [],
  template: '',
})
export class UploadDropzone {}
