services:
  api:
    # --reload restarts uvicorn on source changes — a development convenience,
    # not something the production compose file should ever carry by default.
    # Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
    command: uvicorn api.app:app --host 0.0.0.0 --port 8000 --reload
